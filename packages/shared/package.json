{
  "name": "shared",
  "version": "0.0.1",
  "private": true,
  "description": "前后端共用类型定义与工具函数",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
